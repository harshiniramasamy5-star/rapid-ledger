---
title: Error Reference
description: Standard error format and all error codes
---

## Error Format

All errors follow this shape:

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or expired token"
  }
}
```

## Error Codes

| Code | HTTP | Description |
|------|------|-------------|
| `UNAUTHORIZED` | 401 | Missing, invalid, or expired token |
| `EMAIL_NOT_VERIFIED` | 403 | Account email not yet verified |
| `FORBIDDEN` | 403 | Authenticated but insufficient permissions |
| `NOT_FOUND` | 404 | Resource does not exist |
| `CONFLICT` | 409 | Duplicate resource (e.g. email already registered) |
| `VALIDATION_ERROR` | 400 | Request body failed validation |
| `TOTP_REQUIRED` | 403 | Login requires TOTP code |
| `INVALID_TOTP` | 401 | TOTP code incorrect |
| `INVITE_EXPIRED` | 410 | Invite token expired or already used |
| `INTERNAL_ERROR` | 500 | Unexpected server error |

## Auth Headers

All protected endpoints require:

```
Authorization: Bearer <jwt_token>
Content-Type: application/json
```
