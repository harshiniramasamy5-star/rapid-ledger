-- Enum types RiskLevel, DocumentStatus, RoleType, AuditAction already exist in DB
-- from 20260526193101_init. This migration updates the Prisma schema to declare
-- them explicitly so the generated client handles enum serialization correctly.
-- No SQL changes required.
SELECT 1;
