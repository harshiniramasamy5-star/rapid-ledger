{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd apps/api && npm install --force && npx prisma generate --schema=../../prisma/schema.prisma"
  },
  "deploy": {
    "startCommand": "cd apps/api && npx prisma generate --schema=../../prisma/schema.prisma && npx tsx src/index.ts",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckTimeout": 60
  }
}
