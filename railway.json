{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm install --include=dev && ./node_modules/.bin/prisma generate --schema=prisma/schema.prisma && cd apps/api && npm install --include=dev && npx tsc --noEmit"
  },
  "deploy": {
    "startCommand": "cd apps/api && bun src/index.ts",
    "healthcheckPath": "/health",
    "restartPolicyType": "ON_FAILURE",
    "healthcheckTimeout": 60
  }
}